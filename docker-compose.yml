version: "3"
services:
  frontend:
    build: ./frontend
    ports:
      - "4200:80"

    environment:
      - NODE_ENV=production

  backend:
    build: ./backend
    ports:
      - "3000:3000"

    environment:
      - NODE_ENV=production

      - API_URL=http://localhost:3000
