services:
  backend:
    hostname: backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"

    environment:
      - NODE_ENV=production

  frontend:
    hostname: frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "4000:80"

    environment:
      - NODE_ENV=production
