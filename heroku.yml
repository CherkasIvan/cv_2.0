build:
  docker:
    backend:
      dockerfile: Dockerfile.backend
      context: ./backend
    frontend:
      dockerfile: Dockerfile.frontend
      context: ./frontend
    nginx:
      dockerfile: Dockerfile.nginx
      context: ./nginx
